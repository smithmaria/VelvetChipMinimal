# Velvet Chip (Minimal)
This a minimal web application for a small, mock MongoDB retail database.

## Setup
Requires .env file in /server with `MONGODB_URI` variable.

1. Install dependencies
```
npm install
```
2. Run application
```
npm run dev
```
3. Run server in separate terminal
```
cd server && node index.js
```